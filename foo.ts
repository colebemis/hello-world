// Write TypeScript code that exports a foo function
export function foo(arg: string): string {
  // The foo function should take a string argument and return a string
  // The foo function should concatenate the argument with "foo" and return the result
  return arg + 'foo'
}
